spring:
  datasource:
    url: jdbc:mysql://mysql:3306/userdb
    username: root
    password: root

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true
