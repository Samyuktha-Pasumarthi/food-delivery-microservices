spring:
  datasource:
    url: jdbc:mysql://mysql:3306/foodcataloguedb
    username: root
    password: root
    hikari:
      initialization-fail-timeout: 60000

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true